#include <stdio.h>
#include "single_lists.h"

#define ELEMENTS 4

int main () {
  // int value, i;
  // Tlist *list, *s;

  // list = create();

  // for (i = 1; i <= ELEMENTS; i++) {
  //   printf ("Insira um elemento: ");
  //   scanf ("%d", &value);
  //   list = ordered_insert (list, value);
  //   print_list (list);
  //   printf ("++++++++++++++++++++++\n");
  // }

  // s = search (list, 5);

  // if (s) {
  //   printf ("Elemento %d encontrado\n", s->info);
  // }
  // else {
  //   printf ("Elemento não encontrado\n");
  // }

  // list = remove_elem (list, 5);
  // print_list (list);
  // printf ("++++++++++++++++++++++\n");

  // clear (list);

  random_draw ();

  return 0;
}